# Configuração do interpretador de Python do ambiente virtual
python-interpreter-path = ".venv/Scripts/python.exe"
site-package-path = [".venv/Lib/site-packages"]

# Caminho de busca para módulos do projeto (resolve importações a partir da raiz)
search-path = ["."]

# Desativa a exibição de erros de tipo estático na IDE para evitar falsos positivos
disable-type-errors-in-ide = true

# Diretórios incluídos na análise do linter
project-includes = ["src", "scripts", "main.py"]

# Diretórios excluídos da análise para evitar falsos positivos
project-excludes = [
    "**/.venv/**",
    "**/venv/**",
    "**/__pycache__/**",
    "**/converted/**"
]
